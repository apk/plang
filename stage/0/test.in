# $Header$

var s = "adf";
let a = 1;

print s;

let b = fun (x) {
	print x;
	(2 * x);
};

print b (4);

print (b) (fun (x) { x; } (a));

fun (x,y) {
	print x;
	print y;
} ("xx", "yy");

print "test";

fun (e) {
	print e;
	e ();
} (fun () { print "x"; });

let r = fun () {
	let f = "local-f";
	fun () { f; };
} ();

print r ();


# (print c ((a,b)))
